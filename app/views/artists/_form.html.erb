<%= form_for @artist do |f| %>

  <% if @artist.errors.any? %>
    <div id="error-explanation">
      <h2>
        <%= pluralize(@article.errors.count, "erro") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @artist.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :genre %><br>
    <%= f.text_field :genre %>
  </p>

  <p>
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </p>

  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %><br>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
